// Load the jest-dom matcher type augmentations (toBeInTheDocument, toHaveAttribute, …)
// into the type-check program so component tests type-check against them.
import '@testing-library/jest-dom/vitest';
